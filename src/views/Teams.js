import React from 'react';
import PropTypes from 'prop-types';

const Teams = props => {
    return (
        <div>
            TEAMS
        </div>
    );
};

Teams.propTypes = {
    
};

export default Teams;